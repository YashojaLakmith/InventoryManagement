# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: inventory-management
services:  
  app:
    language: dotnet
    project: ./InventoryManagement.AppHost.csproj
    host: containerapp
